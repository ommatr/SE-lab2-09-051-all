package src;

public class V9 {
    public static double urav(double elem1, double elem2, double elem3) {
        double result = Math.abs(Math.sin(elem1))*Math.cos(elem1/2.)+Math.sqrt(Math.pow(elem2,2)+Math.pow(elem3,2));
        return result;
    }
}
